name: Run Jest Tests on Releases

on:
  push:
    branches:
      - releases
  pull_request:
    branches:
      - releases

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Run Jest Tests
        run: npm test
